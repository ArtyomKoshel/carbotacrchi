version: '3.8'

services:
  nginx:
    image: nginx:1.25-alpine
    container_name: carbot_nginx
    ports:
      - "8080:80"
    volumes:
      - ./laravel/public:/var/www/html/public:ro
      - ./miniapp:/var/www/html/miniapp:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php
    networks:
      - carbot
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: carbot_redis
    restart: unless-stopped
    networks:
      - carbot

  php:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    container_name: carbot_php
    volumes:
      - ./laravel:/var/www/html
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/custom.ini:ro
      - parser_logs:/app/logs:ro
    environment:
      APP_ENV: ${APP_ENV:-local}
      APP_DEBUG: ${APP_DEBUG:-true}
      APP_URL: ${APP_URL:-http://localhost:8080}
      MINIAPP_URL: ${MINIAPP_URL:-}
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: ${DB_DATABASE:-auction_bot}
      DB_USERNAME: ${DB_USERNAME:-carbot}
      DB_PASSWORD: ${DB_PASSWORD:-carbot_pass}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-change_me_secret}
      PARSER_LOG_FILE: /app/logs/parser.log
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CACHE_STORE: redis
      QUEUE_CONNECTION: redis
      LOG_CHANNEL: stderr
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - carbot
    restart: unless-stopped

  mysql:
    image: mysql:8.0
    container_name: carbot_mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root_pass}
      MYSQL_DATABASE: ${DB_DATABASE:-auction_bot}
      MYSQL_USER: ${DB_USERNAME:-carbot}
      MYSQL_PASSWORD: ${DB_PASSWORD:-carbot_pass}
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot_pass"]
      interval: 5s
      timeout: 10s
      retries: 10
      start_period: 30s
    networks:
      - carbot
    restart: unless-stopped

  parser:
    build: ./parser
    container_name: carbot_parser
    volumes:
      - ./parser:/app
      - parser_logs:/app/logs
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: ${DB_DATABASE:-auction_bot}
      DB_USERNAME: ${DB_USERNAME:-carbot}
      DB_PASSWORD: ${DB_PASSWORD:-carbot_pass}
      KBCHA_ENABLED: ${KBCHA_ENABLED:-true}
      KBCHA_INTERVAL_MINUTES: ${KBCHA_INTERVAL_MINUTES:-60}
      KBCHA_MAX_PAGES: ${KBCHA_MAX_PAGES:-1}
      KBCHA_PROXY: ${KBCHA_PROXY:-}
      KBCHA_PROXY_LIST: ${KBCHA_PROXY_LIST:-}
      ENCAR_ENABLED: ${ENCAR_ENABLED:-false}
      ENCAR_INTERVAL_MINUTES: ${ENCAR_INTERVAL_MINUTES:-60}
      ENCAR_MAX_PAGES: ${ENCAR_MAX_PAGES:-1}
      ENCAR_SCHEDULE: ${ENCAR_SCHEDULE:-}
      ENCAR_PROXY: ${ENCAR_PROXY:-}
      USD_KRW_RATE: ${USD_KRW_RATE:-1350}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      REDIS_HOST: redis
      REDIS_PORT: 6379
    command: sleep infinity
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - carbot
    restart: unless-stopped

networks:
  carbot:
    driver: bridge

volumes:
  mysql_data:
  parser_logs:
